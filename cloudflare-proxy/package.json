{
  "name": "mindcore-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MindCore AI key-holding proxy (OpenAI + ElevenLabs) on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^5.10.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
