# RENAMED -- this workflow is now .github/workflows/male_pipeline.yml
# Schedule removed. Manual trigger only, for reference.
name: DEPRECATED - Male Pipeline (see male_pipeline.yml)
on:
  workflow_dispatch:
    inputs:
      note:
        description: "This workflow is deprecated. Use male_pipeline.yml instead."
        required: false
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. The active workflow is male_pipeline.yml"
