# RENAMED -- this workflow is now .github/workflows/female_pipeline.yml
# Schedule removed. Manual trigger only, for reference.
name: DEPRECATED - Female Pipeline (see female_pipeline.yml)
on:
  workflow_dispatch:
    inputs:
      note:
        description: "This workflow is deprecated. Use female_pipeline.yml instead."
        required: false
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. The active workflow is female_pipeline.yml"
